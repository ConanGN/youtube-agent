# YouTube表格字幕列长文本编辑功能开发计划

## 项目概述
为YouTube视频管理系统的字幕列增加长文本编辑功能，支持双击单元格弹出编辑窗口，提供完整的多行文本编辑体验。

## 当前项目状态分析
- **技术栈**: Next.js + TypeScript + Tailwind + TanStack Table
- **现有字幕功能**: 已实现字幕获取、搜索、状态展示
- **现有编辑组件**: 已有`SubtitleEditDialog`组件，可复用架构
- **表格架构**: 使用TanStack Table，支持自定义列定义和单元格渲染

## 1. 技术依赖项分析

### 1.1 现有依赖评估 ✅
- [ ] **无需新增依赖项** - 所需功能均可通过现有技术栈实现
  - React 18 - 支持状态管理和事件处理
  - TanStack Table - 提供灵活的单元格渲染机制
  - Tailwind CSS - 提供完整的UI样式支持
  - Lucide React - 图标支持

### 1.2 代码复用机会分析 ✅
- [ ] **复用SubtitleEditDialog组件架构** - 已有完整的弹窗编辑器实现
- [ ] **复用EditableCell逻辑模式** - 参考现有可编辑单元格模式
- [ ] **复用表格状态管理** - 利用现有的数据更新机制

## 2. UI组件设计要求

### 2.1 单元格显示设计 📱
- [ ] **文本截断显示**
  - 单元格内显示字幕内容前2-3行（约100-150字符）
  - 超出部分显示"...查看更多"提示
  - 保持表格行高一致性

- [ ] **视觉交互提示**
  - 双击触发区域明确标识
  - Hover状态显示编辑提示
  - 字幕状态图标（已有字幕/无字幕/加载中）

### 2.2 编辑弹窗设计 🎨
- [ ] **复用现有SubtitleEditDialog**
  - 弹窗大小: 最大宽度4xl，高度90vh
  - 响应式设计，支持移动端
  - 模态遮罩，点击外部可取消

- [ ] **编辑器功能增强**
  - 多行文本域，自动高度调整（8-30行）
  - 字符计数显示
  - 实时预览前5行内容
  - 保存/取消按钮，快捷键支持

## 3. 具体开发步骤

### 3.1 阶段1: 单元格渲染优化 📊
- [ ] **修改字幕列cell渲染函数**
  - 位置: `YouTubeTable.tsx` 第562-614行 `subtitleColumn.cell`
  - 添加双击事件处理器
  - 实现文本截断显示逻辑
  - 添加编辑状态视觉提示

- [ ] **实现字幕内容提取函数**
  ```typescript
  const extractSubtitleText = (subtitles: any): string => {
    // 从subtitles.cues数组提取纯文本内容
    // 格式化为可编辑的文本格式
  }
  ```

### 3.2 阶段2: 编辑状态管理 🔄
- [ ] **添加编辑状态管理**
  ```typescript
  const [editingSubtitle, setEditingSubtitle] = useState<{
    rowId: string
    isOpen: boolean
    value: string
    originalData: any
  } | null>(null)
  ```

- [ ] **实现双击触发逻辑**
  ```typescript
  const handleSubtitleDoubleClick = (rowId: string, subtitles: any) => {
    const textContent = extractSubtitleText(subtitles)
    setEditingSubtitle({
      rowId,
      isOpen: true,
      value: textContent,
      originalData: subtitles
    })
  }
  ```

### 3.3 阶段3: 数据更新机制 💾
- [ ] **实现保存处理函数**
  ```typescript
  const handleSubtitleSave = (rowId: string, newText: string) => {
    // 1. 将文本转换回字幕格式
    // 2. 更新表格数据
    // 3. 触发onDataChange回调
    // 4. 关闭编辑弹窗
  }
  ```

- [ ] **文本格式转换逻辑**
  ```typescript
  const textToSubtitleFormat = (text: string): SubtitleCues => {
    // 解析文本为时间戳和内容
    // 重建cues数组结构
    // 保持原始格式兼容性
  }
  ```

### 3.4 阶段4: 组件集成 🔗
- [ ] **集成SubtitleEditDialog**
  - 在YouTubeTable组件底部添加编辑弹窗
  - 传递正确的props和事件处理器
  - 确保弹窗层级正确（z-index: 50）

- [ ] **优化表格性能**
  - 使用useMemo优化列定义
  - 避免不必要的重渲染
  - 确保编辑操作不影响其他行

## 4. 数据流和状态管理

### 4.1 数据流设计 🌊
```
用户双击单元格 
  ↓
触发handleSubtitleDoubleClick
  ↓
设置editingSubtitle状态
  ↓
SubtitleEditDialog显示
  ↓
用户编辑并保存
  ↓
handleSubtitleSave处理
  ↓
更新tableData状态
  ↓
触发onDataChange回调
  ↓
表格重新渲染
```

### 4.2 状态管理策略 📁
- [ ] **本地状态管理** - 编辑状态使用组件内部useState
- [ ] **数据持久化** - 通过onDataChange回调向上层传递变更
- [ ] **错误处理** - 编辑失败时恢复原始数据
- [ ] **性能优化** - 使用React.memo和useMemo减少重渲染

## 5. 测试要求

### 5.1 功能测试 🧪
- [ ] **双击触发测试**
  - 双击字幕单元格能正确弹出编辑弹窗
  - 弹窗显示当前字幕内容
  - 不同字幕状态（有/无/加载中/错误）的处理

- [ ] **编辑功能测试**
  - 多行文本编辑正常工作
  - 字符计数准确显示
  - 预览功能正确显示前5行

- [ ] **保存取消测试**
  - 保存后数据正确更新到表格
  - 取消编辑恢复原始内容
  - 快捷键（Ctrl+Enter，ESC）正常工作

### 5.2 用户体验测试 👥
- [ ] **响应式测试** - 在不同屏幕尺寸下弹窗显示正常
- [ ] **性能测试** - 大量数据时编辑操作流畅
- [ ] **错误处理测试** - 异常情况下用户界面友好

### 5.3 集成测试 🔧
- [ ] **表格功能兼容测试** - 编辑功能不影响排序、过滤、选择等
- [ ] **数据同步测试** - 编辑后的数据能正确保存和传递
- [ ] **多实例测试** - 多个表格实例时编辑状态隔离

## 6. Definition of Done (DoD) 标准

### 6.1 功能完整性 ✅
- [ ] **核心功能** - 双击字幕单元格弹出编辑窗口
- [ ] **编辑体验** - 支持多行文本编辑，自动高度调整
- [ ] **数据持久** - 编辑后数据正确保存到表格状态
- [ ] **用户反馈** - 保存/取消操作有明确的用户反馈

### 6.2 代码质量 🏗️
- [ ] **最小修改原则** - 复用现有组件和架构，修改量最小化
- [ ] **TypeScript支持** - 所有新增代码有完整的类型定义
- [ ] **代码注释** - 关键逻辑有中文注释说明
- [ ] **错误处理** - 完善的错误处理和边界情况处理

### 6.3 性能和体验 ⚡
- [ ] **响应性能** - 编辑操作响应时间 < 200ms
- [ ] **视觉一致** - 与现有UI风格保持一致
- [ ] **无副作用** - 编辑操作不影响其他表格功能
- [ ] **向后兼容** - 不破坏现有的字幕获取和搜索功能

### 6.4 测试覆盖 🎯
- [ ] **手动测试** - 所有功能路径手动验证通过
- [ ] **边界测试** - 空字幕、长字幕、特殊字符等场景测试
- [ ] **浏览器兼容** - 主流浏览器测试通过
- [ ] **回归测试** - 现有功能未受影响

## 7. 实现优先级

### 7.1 P0 (核心功能) 🔴
1. 字幕单元格双击触发编辑
2. 复用SubtitleEditDialog实现编辑弹窗
3. 保存编辑结果到表格数据

### 7.2 P1 (用户体验) 🟡
1. 单元格文本截断显示优化
2. 编辑状态视觉反馈
3. 键盘快捷键支持

### 7.3 P2 (体验增强) 🟢
1. 字幕格式智能识别
2. 编辑历史记录
3. 批量编辑功能

## 8. 风险评估与缓解

### 8.1 技术风险 ⚠️
- **数据格式兼容性** - 现有字幕数据结构可能复杂
  - 缓解: 先实现简单文本编辑，逐步增强格式支持
- **性能影响** - 大量数据时编辑可能影响表格性能
  - 缓解: 使用React优化技术，编辑状态隔离

### 8.2 用户体验风险 📱
- **移动端适配** - 弹窗在小屏幕上可能显示异常
  - 缓解: 复用现有响应式设计，充分测试移动端
- **操作冲突** - 双击可能与其他表格操作冲突
  - 缓解: 精确控制事件触发范围

## 9. 交付物

### 9.1 代码交付
- [ ] 更新的 `YouTubeTable.tsx` 文件
- [ ] 必要的类型定义更新
- [ ] 功能测试用例

### 9.2 文档交付
- [ ] README.md 更新记录
- [ ] 技术实现说明
- [ ] 用户使用指南

---

**估计开发时间**: 4-6小时  
**风险等级**: 低 (复用现有架构)  
**技术难度**: 中等  
**用户价值**: 高 (显著改善字幕编辑体验)